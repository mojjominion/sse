{
  "name": "sse",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "eslint": "^8.22.0"
  },
  "scripts": {
    "listen": "curl -H Accept:text/event-stream http://localhost:3000/events",
    "post": "curl -X POST -H 'Content-Type: application/json' -s http://localhost:3000/fact -d"
  }
}
